play.http.secret.key = "changeme"

play.i18n {
  langs = [ "en" ]
}

play.assets {
  path = "/public"
  urlPrefix = "/assets"
}

slick.dbs.default {
  profile = "slick.jdbc.PostgresProfile$"
  driver = "slick.driver.PostgresDriver$"
  db {
    url = "jdbc:postgresql://localhost:5434/seed"
    user = "postgres"
    password = "postgres"
    driver = "org.postgresql.Driver"
  }
}
