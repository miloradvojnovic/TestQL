version: '3'

services:
  postgres:
    image: postgres:9.6.1-alpine
    restart: always
    ports:
    - "5434:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: seed